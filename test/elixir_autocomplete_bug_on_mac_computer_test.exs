defmodule ElixirAutocompleteBugOnMacComputerTest do
  use ExUnit.Case
  doctest ElixirAutocompleteBugOnMacComputer

  test "greets the world" do
    assert ElixirAutocompleteBugOnMacComputer.hello() == :world
  end
end
